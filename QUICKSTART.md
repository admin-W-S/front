# 빠른 시작 가이드

## 프로젝트 설정

### 1단계: 의존성 설치
터미널에서 다음 명령어를 실행하세요:

```bash
npm install
```

### 2단계: 개발 서버 실행
```bash
npm run dev
```

브라우저에서 `http://localhost:5173` (또는 표시된 URL)로 접속하세요.

### 3단계: 환경 변수 설정
`.env` 파일을 생성하세요:

```bash
# .env 파일 생성
echo VITE_DEV_MODE=true > .env
```

**💡 백엔드 없이 테스트하기**: `VITE_DEV_MODE=true`로 설정하면 Mock 데이터로 모든 기능을 테스트할 수 있습니다!

**백엔드가 있는 경우**:
```bash
# .env 파일
VITE_API_BASE_URL=http://localhost:3000/api
```

## 주요 페이지

- **/** - 홈 페이지
- **/login** - 로그인 (학생/관리자)
- **/signup** - 회원가입
- **/classrooms** - 강의실 목록 (로그인 필요)
- **/reserve** - 예약 생성 (학생만)
- **/my-reservations** - 내 예약 내역 (학생만)
- **/admin/classrooms** - 강의실 관리 (관리자만)

## 테스트 계정

백엔드 API가 준비되지 않은 경우, 로컬 스토리지에서 테스트 데이터를 확인할 수 있습니다:

1. 개발자 도구 (F12) 열기
2. Application > Local Storage 이동
3. `user`와 `role` 키 확인

## 문제 해결

### npm install 오류
```bash
# PowerShell 실행 정책 문제인 경우
Set-ExecutionPolicy -ExecutionPolicy Bypass -Scope Process

# 또는 관리자 권한으로
npm install --force
```

### 포트 충돌
```bash
# 다른 포트로 실행
npm run dev -- --port 3001
```

